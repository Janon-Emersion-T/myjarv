# Agents

Agent prompts live in `packages/agents/prompts`.

Registry metadata lives in `packages/agents/registry.json`.

The prompt set has been standardized with canonical sections while preserving the original specialist prompt bodies under `## Legacy Profile`.

