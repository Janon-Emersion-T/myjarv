# Knowledge Loader

The knowledge loader reads structured markdown files from `data/knowledge` and supports:

* category listing
* simple relevance retrieval

This is the first local-first step before adding vector-backed retrieval later.

